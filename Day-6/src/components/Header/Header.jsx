import React from 'react'
import { Container, Logo, LogoutBtn } from "../index.js";
import { Link } from 'react-router-dom';

/**
 * useSelector()
 * Tabhi dekh payenge from store user login hai or logout 
 * 
 */
// Render conditionally
// Agar User loggedIn then show logOut button
// We can put the Header into Container



const Header = () => {
  return (
    <div>Header</div>
  )
}

export default Header