// We are doing server call to get weather information
// Returns: sunny, rainy, cloudy

let weather = "rainy";

if (weather === "rainy") {
  console.log(`its raining, Carry umbrella with you`);
} else if (weather === "cloudy") {
  console.log(`Its cloudy, Carry jacket with you`);
} else {
  console.log(`Chill the sunny day`);
}
